# Admin API Configuration
# Copy this file to .env.local and set your admin key

# Generate a strong admin key for accessing admin endpoints
# You can use: openssl rand -hex 32
ADMIN_API_KEY=your-super-secret-admin-key-here

# This key will be used in the x-admin-key header when calling admin endpoints
# Example usage:
# curl -H "x-admin-key: your-key" http://localhost:3000/api/admin/stats
